namespace eMarket.SharedKernel.Constants;

public static class Policies
{
    public const string RequireAdmin = nameof(RequireAdmin);

    public const string RequireSeller = nameof(RequireSeller);

    public const string RequireCustomer = nameof(RequireCustomer);

    public const string RequireDeliveryDriver = nameof(RequireDeliveryDriver);
}